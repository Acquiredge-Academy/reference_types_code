let student = {
    firstName: "Jeff",
    lastName: "Winters"
};

// let sentence = "The student's full name is " + student.firstName + " " + student.lastName + ".";
let sentence = `The student's full name is ${student.firstName} ${student.lastName}.`;
console.log(sentence);